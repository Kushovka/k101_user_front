- Нужно сделать на бэке новую ручку для создания пользователей. POST-эндпоинт, например /admin/users/create, который принимает JSON с полями(username, email, first_name, last_name, role по дефолту "user"), как у всех пользователей в таблице. Доступ к ручке только для админов

- Сделать новую ручку пример upload/status/${task_id} что бы видеть статус загруженного файла в адмиинке

- сделать ручку для новой страницы тарифов

  GET /plans — получить все доступные тарифы

  POST /plans — создать тариф (админ)
  (цена тарифа (price), время действия плана в месяцах(month), название плана(plan_name) - пока что так)

  PUT /plans/:id — редактировать тариф (админ)
  (изменяю цену тарифа (price), время действия плана в месяцах(month), название плана(plan_name))

  PATCH /plans/:id — архивировать тариф (админ)

в свойства должны быть id и archived по дефолту false, через PATCH я буду менять это свойсто на true и на фронте уже отображать это все как надо
